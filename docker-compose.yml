version: '3.8'

services:
  land-litigation:
    build: .
    container_name: dclm-land-litigation
    restart: unless-stopped
    ports:
      - "8010:8010"
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge